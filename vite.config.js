import { defineConfig } from 'vite';

export default defineConfig({
  // Twoje niestandardowe ustawienia Vite
  // server: {
  //   port: 3001,
  //   strictPort: true,
  // },
  build: {
    outDir: 'dist',
  },
});
